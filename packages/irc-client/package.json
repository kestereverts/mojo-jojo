{
  "name": "@mojo-jojo/irc-client",
  "version": "0.1.0",
  "description": "Modern, RxJS-first IRCv3 client: connection, state tracking, and rich reactive events.",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mojo-jojo/irc-message": "workspace:*",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "rxjs-marbles": "^7.0.0"
  }
}
